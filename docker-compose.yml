services:
  db:
    image: postgres:16
    container_name: indexar_db
    environment:
      POSTGRES_USER: indexar
      POSTGRES_PASSWORD: indexar_dev_pass
      POSTGRES_DB: indexar
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
